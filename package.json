{
  "name": "kartonjs",
  "version": "1.0.4",
  "main": "KartonElement.js",
  "scripts": {
    "dev": "vite",
    "test": "vitest --exclude backup/",
    "deploy": "git add . --all && git commit -m new || true && surge ./ && npm publish"
  },
  "keywords": [
    "karton",
    "element",
    "web-component",
    "framework"
  ],
  "author": "Biensure",
  "license": "MIT",
  "devDependencies": {
    "@testing-library/dom": "^10.4.0",
    "jsdom": "^26.1.0",
    "vite": "^6.3.5",
    "vitest": "^3.2.2"
  },
  "dependencies": {
    "uhtml": "^4.7.1"
  }
}
